<div class="container">
  <div class="row">
    <div class="col-md-12 col-lg-12">
      <%= render 'users/wellcome' %>
    </div>
    <div class=" col-lg-2 col-md-3">
    <%if user_signed_in? %>
      <table class="table table-bordered table-hover">
        <tbody>
          <tr>
            <% current_user.startups.each do |startups| %>
            <td>
              <%=link_to "My startup", startups, class:"text-dark"  %>
            </td>
              <% end %>
          </tr>
          <tr>
            <td> Messages room</td>
         </tr>
         <tr>
           <td> My network</td>
        </tr>
         <tr>
           <td><%= link_to 'Expand my network', users_path,class:"text-dark" %></td>
        </tr>
      </tbody>
    </table>
  <% end %>
  </div>
  <div class="col-lg-10 col-md-9">
      <% @startups.each do |startup| %>
            <div class="row">
                <%= image_tag startup.banner_thumbnail, class:"img-fluid", style:'width="100%";'%>
                <table class="table table-bordered">
                  <tbody>
                    <tr>
                      <td colspan="6"><%= startup.resume %></td>
                    </tr>
                    <tr>
                      <td><strong>Name</strong> : <%= startup.name %> </td>
                      <td><strong>Contact</strong> : <%= startup.contact %> </td>
                      <td><strong>adress</strong> : <%= startup.adresse %> </td>
                      <td><strong><%= startup.sector_of_business %></strong> startup  </td>
                      <td><strong>favorit.count</strong></td>
                      <td> <%= link_to 'show stratup', startup,class:"btn btn-sm btn-info" %></td>
                    </tr>
                </tbody>
              </table>
            </div>
      <% end %>
    </div>
  </div>
</div>
